name: voice
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  # mic_stream: ^0.7.2
  flutter_sound: ^9.28.0
  path_provider: ^2.1.5
  permission_handler: ^12.0.1
  tflite_flutter: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/model/deep_speaker.tflite
